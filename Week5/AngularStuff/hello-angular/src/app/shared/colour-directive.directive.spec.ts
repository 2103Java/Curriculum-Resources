import { ColourDirectiveDirective } from './colour-directive.directive';

describe('ColourDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ColourDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
